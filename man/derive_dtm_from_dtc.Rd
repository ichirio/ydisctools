% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conv_iso8601.R
\name{derive_dtm_from_dtc}
\alias{derive_dtm_from_dtc}
\title{Derive POSIXct Datetime from Partial ISO 8601 Datetime String}
\usage{
derive_dtm_from_dtc(dtc, inpute_type = "first", tz = "")
}
\arguments{
\item{dtc}{A character vector containing partial ISO 8601 datetime strings (e.g., "2023", "2023-01", "2023-01-01T12").}

\item{inpute_type}{A character string specifying how to impute missing components.
\itemize{
\item \code{"first"}: Impute the earliest possible values (e.g., "2023" becomes "2023-01-01T00:00:00").
\item \code{"last"}: Impute the latest possible values (e.g., "2023" becomes "2023-12-31T23:59:59").
Default is \code{"first"}.
}}

\item{tz}{A character string specifying the time zone for the resulting \code{POSIXct} object. Default is an empty string (\code{""}), which uses the system's default time zone.}
}
\value{
A \code{POSIXct} vector representing the complete datetime values.
If \code{dtc} contains invalid datetime strings, an error is raised.
}
\description{
This function converts a partial ISO 8601 datetime string (\code{DTC}) into a complete \code{POSIXct} datetime object.
Missing components (e.g., month, day, time) are imputed based on the specified \code{inpute_type}.
}
\details{
The function validates the input \code{dtc} against a regular expression to ensure it conforms to the ISO 8601 format.
Missing components are imputed based on the \code{inpute_type} parameter:
\itemize{
\item \code{"first"}: Imputes the earliest possible values (e.g., "2023" becomes "2023-01-01T00:00:00").
\item \code{"last"}: Imputes the latest possible values (e.g., "2023" becomes "2023-12-31T23:59:59").
}
}
\examples{
# Example 1: Impute earliest possible values
derive_dtm_from_dtc(c("2023", "2023-01", "2023-01-01"), inpute_type = "first")

# Example 2: Impute latest possible values
derive_dtm_from_dtc(c("2023", "2023-01", "2023-01-01"), inpute_type = "last")

# Example 3: Handle time zone
derive_dtm_from_dtc(c("2023-01-01T12:34:56"), tz = "UTC")

}
