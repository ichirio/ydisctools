% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/listing_wrap.R
\name{listing_wrap}
\alias{listing_wrap}
\title{Wrap a listing cell to a display width}
\usage{
listing_wrap(text, width, sep = "/")
}
\arguments{
\item{text}{The cell text. A character vector is allowed; each element is
wrapped independently.}

\item{width}{Maximum display width in characters. \code{NULL}, \code{NA} or a value
\verb{<= 0} disables wrapping and returns the text unchanged.}

\item{sep}{The separator to break after (default \code{"/"}). \code{""} or \code{NULL}
skips priority 1 and wraps on word boundaries only.}
}
\value{
For a \code{text} of length 1, a character vector of lines. For a longer
\code{text}, a list of such character vectors, one per element.
}
\description{
Breaks one cell of a clinical listing into the physical lines it occupies,
following the listing convention: break \strong{after the separator} first, then
at word boundaries, and only then hard-split a token that is still too long.
This is the wrapping step \code{\link[=rtf_listing]{rtf_listing()}} applies to every cell; it is
exported so you can measure, preview or reuse it on its own.
}
\details{
\code{width} is a \strong{display width in characters}, not a character count: a
full-width (CJK) glyph counts as two, so a Japanese listing wrapped to
\code{width = 20} really does fit in twenty monospaced columns. \code{nchar()} would
count those glyphs as one apiece and overflow the column.
}
\section{Break priority}{

\enumerate{
\item \strong{After \code{sep}.} \code{"Ovarian cancer/BRCA1 mutation"} prefers to break
between its components, and the separator stays at the end of the line it
closes -- the look a stacked listing column is expected to have.
\item \strong{At a word boundary} -- after whitespace, a comma or a hyphen.
\item \strong{Hard split}, for a single token wider than \code{width} on its own (a long
identifier, a URL). Every returned line is then guaranteed to fit.
}
}

\examples{
listing_wrap("Ovarian cancer/BRCA1 mutation/Serous adenocarcinoma", 22)

# An over-long token is hard-split rather than overflowing the column.
listing_wrap("Immunohistochemistry", 8)

# Vectorised: one element in, one character vector out.
listing_wrap(c("Partial response", "Progressive disease"), 10)

# width = NULL leaves the cell alone.
listing_wrap("Ovarian cancer/BRCA1 mutation", NULL)
}
\seealso{
\code{\link[=rtf_listing]{rtf_listing()}}, which applies this to every cell;
\code{\link[=auto_listing_widths]{auto_listing_widths()}} for choosing \code{width};
\code{\link[=split_column_by_max_bytes]{split_column_by_max_bytes()}} for the byte-budget (SAS transport) split,
which solves a different problem.
}
