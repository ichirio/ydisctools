% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot_stats_wider.R
\name{pivot_stats_wider}
\alias{pivot_stats_wider}
\title{Expand one or more columns into combined column headers}
\usage{
pivot_stats_wider(
  data,
  expand_cols,
  value_col = "fmt_value",
  sep = "____",
  names_sort = FALSE
)
}
\arguments{
\item{data}{A data frame in long form.}

\item{expand_cols}{Character vector naming one or more columns to spread into
the header. Their values (joined by \code{sep}) become the new column names.}

\item{value_col}{String. Name of the column whose values fill the new cells.
Default \code{"fmt_value"}.}

\item{sep}{String used to join the \code{expand_cols} values into a header name.
Default \code{"____"} (four underscores).}

\item{names_sort}{Logical. If \code{TRUE}, the new columns are sorted by name;
if \code{FALSE} (default) they keep the order in which the combinations first
appear in \code{data}.}
}
\value{
A data frame (tibble): the row-identifier columns (everything except
\code{expand_cols} and \code{value_col}) followed by one column per spread
combination.
}
\description{
\code{pivot_stats_wider()} reshapes a long summary table to wide form by spreading
one \emph{or more} columns into the header. The spread columns are pasted together
(with \code{sep}) to form each new column name, and the cells are filled from
\code{value_col}. Every remaining column is kept as a row identifier. This is a
thin, opinionated wrapper around \code{\link[tidyr:pivot_wider]{tidyr::pivot_wider()}} tuned for the
formatted statistic tables produced by \code{\link[=format_stats]{format_stats()}}.
}
\details{
For example, spreading \code{column1} and \code{column2} (values \code{cohort1}/\code{cohort2}
and \code{trt1}/\code{trt2}) with the default separator produces the headers
\code{cohort1____trt1}, \code{cohort1____trt2}, \code{cohort2____trt1}, \code{cohort2____trt2}.
The four-underscore default is chosen to be an unlikely substring of real
data, so the combined names can later be split back apart safely.
}
\examples{
df <- data.frame(
  column1 = rep(c("cohort1", "cohort2"), each = 4),
  column2 = rep(rep(c("trt1", "trt2"), each = 2), times = 2),
  group1 = "Hoge",
  group2 = "Sex",
  label = rep(c("Male", "Female"), times = 4),
  fmt_value = "10 (50.0)",
  stringsAsFactors = FALSE
)

pivot_stats_wider(df, expand_cols = c("column1", "column2"))

}
