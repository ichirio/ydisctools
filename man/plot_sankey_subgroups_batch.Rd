% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sankey_subgroups_batch.R
\name{plot_sankey_subgroups_batch}
\alias{plot_sankey_subgroups_batch}
\title{Batch Create Sankey Plots for Subgroup Analysis}
\usage{
plot_sankey_subgroups_batch(
  nodes,
  links,
  subgroup_specs,
  node_id = "id",
  node_stage = "stage",
  node_label = NULL,
  node_value = NULL,
  link_source = "source",
  link_target = "target",
  link_value = "value",
  scale_strategy = c("shared_max", "shared_first_stage", "first_stage_normalized"),
  scale_reference_subgroup = NULL,
  first_stage_max_multiplier = 3,
  output_dir = NULL,
  save_png = TRUE,
  width = 14,
  height = 8,
  dpi = 180,
  bg = "white",
  ...
)
}
\arguments{
\item{nodes}{Node data frame.}

\item{links}{Link data frame.}

\item{subgroup_specs}{Data frame with subgroup definitions.
Recommended columns:
\itemize{
\item \code{subgroup}: subgroup name.
\item \code{filter}: expression string applied to both \code{nodes} and \code{links}.
\item \code{node_filter}: expression string for \code{nodes} only (optional).
\item \code{link_filter}: expression string for \code{links} only (optional).
\item \code{title}: plot title override (optional).
\item \code{file_name}: output file name (optional).
}}

\item{node_id, node_stage, node_label, node_value}{Node mapping columns passed to \code{plot_sankey()}.}

\item{link_source, link_target, link_value}{Link mapping columns passed to \code{plot_sankey()}.}

\item{scale_strategy}{Scale strategy across subgroups:
\code{"shared_max"}, \code{"shared_first_stage"}, or \code{"first_stage_normalized"}.}

\item{scale_reference_subgroup}{Reference subgroup name used when
\code{scale_strategy = "shared_first_stage"}. If \code{NULL}, the first subgroup in
\code{subgroup_specs} is used.}

\item{first_stage_max_multiplier}{Magnification cap used when
\code{scale_strategy = "first_stage_normalized"}. Use \code{Inf} (or \code{NULL}) for no cap.}

\item{output_dir}{Optional output directory to save PNG files.}

\item{save_png}{Whether to write PNG files.}

\item{width, height, dpi, bg}{PNG rendering options for \code{ggsave()}.}

\item{...}{Additional arguments passed to \code{plot_sankey()}.}
}
\value{
A list with:
\itemize{
\item \code{plots}: named list of ggplot objects.
\item \code{metadata}: data frame with subgroup, scales, multipliers, and output file names.
}
}
\description{
Create multiple Sankey plots from subgroup-specific filters in one call.
The function supports scaling strategies across subgroups:
\enumerate{
\item Shared scale across all subgroup plots.
\item First-stage normalization with optional magnification cap, then shared scale.
\item Shared scale anchored to the first-stage span of a reference subgroup.
}
}
