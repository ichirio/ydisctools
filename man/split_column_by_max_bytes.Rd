% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_column_by_max_bytes.R
\name{split_column_by_max_bytes}
\alias{split_column_by_max_bytes}
\title{Split a specified column into multiple columns based on maximum byte length}
\usage{
split_column_by_max_bytes(
  df,
  var_name,
  max_bytes = 200,
  label = NULL,
  keep_org = FALSE,
  splitter = " |;|,|\\\\.|-"
)
}
\arguments{
\item{df}{A data frame containing the column to be split.}

\item{var_name}{A string specifying the name of the column to be split.}

\item{max_bytes}{An integer specifying the maximum byte length for each split. Default is 200.}

\item{label}{A string specifying a label. Must be a character and less than 40 characters. Default is NULL.}

\item{keep_org}{A logical value indicating whether to keep the original column. Default is FALSE.}

\item{splitter}{A regular expression pattern used to identify split points. Default is " |;|,|\\.".}
}
\value{
A data frame with the specified column split into multiple columns.
}
\description{
Split a specified column into multiple columns based on maximum byte length
}
\examples{
df <- data.frame(
  id = 1:3,
  text = c("This is a very long text.", "This is another long text.", "Short text")
)
result <- split_column_by_max_bytes(df, "text", max_bytes = 10)
print(result)
}
