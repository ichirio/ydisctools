% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/listing_widths.R
\name{suggest_listing_widths}
\alias{suggest_listing_widths}
\title{Suggest starting column widths for a listing}
\usage{
suggest_listing_widths(
  data,
  ...,
  total_width = 150,
  min_width = 6,
  probs = 0.9
)
}
\arguments{
\item{data}{A data frame, as passed to \code{\link[=rtf_listing]{rtf_listing()}}.}

\item{...}{\code{\link[=listing_col]{listing_col()}} specifications, in display order.}

\item{total_width}{Total display width available for the content columns, in
characters (default \code{150}, a landscape A4 page in a small monospaced
font). Spacer columns are not counted.}

\item{min_width}{Minimum width any column may be given (default \code{6}).}

\item{probs}{Quantile of each column's cell widths taken as its natural
demand (default \code{0.9}).}
}
\value{
A named integer vector of suggested widths, one per column, in the
order the specifications were given. Printing it shows a pasteable
summary next to each column's measured demand.
}
\description{
Picking the per-column \code{width} values for \code{\link[=listing_col]{listing_col()}} by hand is
tedious: too narrow and cells wrap into a tall, ragged record, too wide and
the listing overflows the page. \code{suggest_listing_widths()} proposes a
starting set from the data, which you then tune by eye against the rendered
output.
}
\details{
The proposal is content-driven: each column is composed exactly as
\code{\link[=rtf_listing]{rtf_listing()}} would compose it, its cell widths are measured (display
width, so wide CJK glyphs count as two), and the \code{probs} quantile of that
distribution is taken as the column's natural demand -- a quantile rather
than the maximum, so one unusually long value does not dominate the layout.
The header's own longest line is respected as a floor, and the demands are
then scaled to fit \code{total_width} and rounded, subject to \code{min_width}.
}
\examples{
adsl <- data.frame(
  USUBJID = c("63016-204", "63016-205"),
  DISPTPD = c("Ovarian cancer", "Fallopian tube cancer"),
  STAGE   = c("IIIC", "IV"),
  stringsAsFactors = FALSE
)
suggest_listing_widths(
  adsl,
  listing_col(USUBJID, header = "Unique\nSubject ID"),
  listing_col(DISPTPD, header = "Primary Diagnosis"),
  listing_col(STAGE,   header = "Stage"),
  total_width = 60
)
}
\seealso{
\code{\link[=listing_col]{listing_col()}}, \code{\link[=rtf_listing]{rtf_listing()}}
}
